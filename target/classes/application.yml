spring:
  application:
    name: teach-record-api
  datasource:
    url: jdbc:mysql://127.0.0.1:3308/teach_record?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: TSWsjO3oq3PUnOt
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 15MB

server:
  port: 8080

app:
  jwt:
    secret: "change-this-to-a-long-random-secret-at-least-256-bits-for-production"
    expiration-ms: 86400000
  upload:
    dir: uploads
  # cors.allowed-origin-patterns 可选；不配则放行全部 Origin（解决生产域名/IP 的 CORS）。
  # 公网建议收紧，例如：cors: { allowed-origin-patterns: ["https://你的域名"] }
