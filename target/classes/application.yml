spring:
  application:
    name: teach-record-api
  datasource:
    url: jdbc:mysql://111.229.234.228:3306/teach_record?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: TSWsjO3oq3PUnOt
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 15MB

server:
  port: 8080

app:
  jwt:
    secret: "change-this-to-a-long-random-secret-at-least-256-bits-for-production"
    expiration-ms: 86400000
  upload:
    dir: uploads
